name: CI

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
    - uses: actions/checkout@v3

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install dependencies
      run: npm ci

    - name: Build project
      run: npm run build

    - name: Check compilation
      run: |
        if [ -d "build" ]; then
          echo "✅ Build successful"
          ls -la build/
        else
          echo "❌ Build failed"
          exit 1
        fi

    - name: Run TypeScript check
      run: npx tsc --noEmit

    - name: Run tests
      run: npm test

    - name: Run tests with coverage
      run: npm run test:coverage

    - name: Check git status
      run: |
        # Ignore build artifacts (*.js, *.js.map) since .gitignore excludes them
        if [ -n "$(git status --porcelain -- '*.ts' '*.json' '*.yml' '*.md')" ]; then
          echo "❌ Source files have uncommitted changes"
          git status
          exit 1
        fi
